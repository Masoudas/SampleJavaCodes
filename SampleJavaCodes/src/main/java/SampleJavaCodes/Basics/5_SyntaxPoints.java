package SampleJavaCodes.Basics;

class SyntaxPoints {
    public void noExpressionAndEvaluationInJava(){
        /**
         * Let's just say we cannot use expressions and evaluations in a single line of Java code,
         * that is to say we cannot write "x" == "x"; 2+3;
         */
    }

    public void methodSignature() {
        /**
         * A method signature includes it's name, together with it's arguments. As we know,
         * it does not contain return type. When we say two methods cannot have same signature,
         * what does it mean? It means two methods cannot have same signature, but different
         * content, or return type.
         */
        
    }

}