/**
 * 
 * So this would be the class path of the maven project.
 * For example, we can put a .jar library in the /lib of the
 * project. Then we add the following lines in the classpath:
 * <classpathentry exported="true" kind="lib" path="lib/a.jar"/>
 *  
 */